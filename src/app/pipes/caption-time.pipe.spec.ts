import { CaptionTimePipe } from './caption-time.pipe';

describe('CaptionTimePipe', () => {
  it('create an instance', () => {
    const pipe = new CaptionTimePipe();
    expect(pipe).toBeTruthy();
  });
});
